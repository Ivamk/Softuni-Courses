# names = input().split(", ")
# result = sorted(names, key=lambda item: (-len(item), item))
# сортира имената първо по тяхната дължина descending order, затова е с минус,
# след това ако две имена са с еднаква дължина да се сортират по възходящ ред

print(list(filter(lambda x: x % 2 == 0, [1, 2, 3, 4])))

print(list(map(lambda x: x*2, [1, 2, 3, 4, 5, 6])))

